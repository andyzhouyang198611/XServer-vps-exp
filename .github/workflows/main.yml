on:
  schedule:
    - cron: 0 7,15,23 * * *
  push:
  workflow_dispatch:

jobs:
  job:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions: # 关键：必须给 Actions 写入权限
      contents: write
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v4
      - run: sudo apt-get -qq update && sudo apt-get -yqq install --no-install-recommends ffmpeg fonts-noto-cjk
      - run: yarn add puppeteer
      - run: node main.mjs
        env:
          EMAIL: ${{ secrets.EMAIL }}
          PASSWORD: ${{ secrets.PASSWORD }}
          PROXY_SERVER: ${{ secrets.PROXY_SERVER }}

      # --- 新增：将修改后的 README.md 提交并推送到仓库 ---
      - name: 提交并更新 README
        if: always() # 确保即使脚本出错也能尝试更新报告（如果有变动）
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          # 检查是否有变化，有变化才提交
          if ! git diff --cached --quiet; then
            git commit -m "Update README with renewal status [skip ci]"
            git push
          else
            echo "README 内容无变化，跳过提交"
          fi
          
      - name: 📱 发送 Telegram 通知
        # 修复后的 if 语法：在 if 内部引用 secrets 需要包裹在 ${{ }} 中
        if: always()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          # 直接构建固定格式的消息
          MESSAGE="🐢 Xserver 运行通知：
          📅 时间：$(TZ='Asia/Shanghai' date '+%Y-%m-%d %H:%M:%S')
          📊 状态：${{ job.status == 'success' && '✅ 成功' || '❌ 失败' }}
          
          📋 详细日志：
          🔗 ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"

          # 发送通知
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            --data-urlencode "chat_id=${TELEGRAM_CHAT_ID}" \
            --data-urlencode "text=${MESSAGE}" \
            -d "parse_mode=Markdown"
      - uses: actions/upload-artifact@v4
        with:
          path: recording.webm
